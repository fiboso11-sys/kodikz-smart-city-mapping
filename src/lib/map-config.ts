/** Free vector tiles — roads visible, no API key (OpenFreeMap / OSM). */
export const FREE_MAP_STYLE = "https://tiles.openfreemap.org/styles/liberty";

export const MAP_VIEW = {
  center: [55.2708, 25.2048] as [number, number],
  zoom: 12,
  pitch: 0,
  bearing: 0,
};
